const str = "ztm is the best of the best"

str.replaceAll('best','worst')           //replaceAll with replaces all the bests in the string
'ztm is the worst of the worst'           

str.replace('best','worst')              //replace will replace only the first best
'ztm is the worst of the best'
